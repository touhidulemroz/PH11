// finding the length of the array 

const ictNum = [10, 20, 30, 40, 50, 60];
console.log(ictNum.length);

// changing the value of the array

const fruits = ['mango' ,'apple', 'banana'];
console.log(fruits);
fruits[0] = 'jackfruits';
console.log(fruits);