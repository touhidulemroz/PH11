/**
 * Declare a function
 * call check whether the function is called properly
 * set a parameter (s)
 * pass the parameter(s) , check whether the parameters is passed in a proper format
 * return the result
 */
function sumOfNumbers() {
    console.log(numbers);
    return
}

const numbers = [20, 21, 45, 46, 78, 90, 49];
const sum = sumOfNumbers(numbers);
console.log(sum);
